@model ServicosEPedidos_Mod_17E.Models.Pedido
@using ServicosEPedidos_Mod_17E.Helper

@{
    ViewBag.Title = "Details";
}
<h2>Detalhes do pedido</h2>
<div>
    <h4>Pedido</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.IdClie.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IdClie.Nome)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Descricao)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.DataPed)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DataPed)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Valor)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Valor)
        </dd>
    </dl>
</div>
<p>
    @if (User.IsInRole("Prestador"))
    {
        <a href="@Url.Action("Create", "PedidoAceites", new { id = Html.UserId(User), ped = ViewBag.Algo })" class="btn btn-primary">Aceitar Pedido</a>
    }
    @if (User.IsInRole("Cliente"))
    {
        @Html.ActionLink("Delete", "Delete", new { id = ViewBag.Algo })
    }

    @Html.ActionLink("Back to List", "Index", "Home")
</p>
